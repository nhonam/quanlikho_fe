import baseReducer from '../BaseReducer';

const DispatchType = "_APP"; // dispatch name
// const ActionType = {
// } // extended action type for customAction
export default baseReducer({
    DispatchType: DispatchType,
    // extendState: {
    // },
    // customAction: (state, action) => {
    //     switch (action.type) {
    //         default:
    //             break
    //     }
    // }
});